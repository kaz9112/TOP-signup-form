make html barebone
like this
    flex container reverse-row

    flex container column

        h2 this is
        h2 you know you

        form
            h2 lets do this

            flex container row
                flex container-column
                    label
                    input name required

                flex container-column
                    label
                    input last name required

            flex container row   
                flex container-column
                    label
                    input email rqeuired
                
                flex container-column
                    label
                    input phone number (number only)

            flex container row (needs to be same as each other, use js)
                label
                input password rqeuired

                label
                input password confirmations (number only)
        
        button create account

        p already have an account? <clickable>log in<clickable>

    Image heroes

make styling




script js
    get password input from html
    get confirm passw input from html

    if confirm passw == ''
        pass
    else if confirm passw != passw, show error
        change border color to red
    else if confirm passw == pass
        change border color to green
